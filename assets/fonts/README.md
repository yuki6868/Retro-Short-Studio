# Fonts

Preview text rendering loads fonts through `LocalFontRepository`.

Place the default Japanese preview font here:

```text
assets/fonts/AkazukiPOP.otf
```

The renderer never uses an absolute OS font path. Replace this file to change the default preview font without editing renderer code.
